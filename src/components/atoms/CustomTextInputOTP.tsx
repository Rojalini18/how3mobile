import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomTextInputOTP = () => {
  return (
    <View>
      <Text>CustomTextInputOTP</Text>
    </View>
  );
};

export default CustomTextInputOTP;

const styles = StyleSheet.create({});
