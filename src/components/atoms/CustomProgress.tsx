import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const CustomProgress = () => {
  return (
    <View>
      <Text>CustomProgress</Text>
    </View>
  );
};

export default CustomProgress;

const styles = StyleSheet.create({});
